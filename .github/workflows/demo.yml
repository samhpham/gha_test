name: Demo
on: [push]
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World 🌎"
      - name: List files in workspace
        run: |
          ls ${{ github.workspace }}